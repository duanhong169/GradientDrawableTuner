<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="top.defaults.gradientdrawabletuner.GradientDrawableViewModel"/>
        <variable
            name="maxRadiusDp"
            type="Integer"/>
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

        <TextView
            android:id="@+id/textView"
            android:layout_width="@dimen/drawableWidth"
            android:layout_height="@dimen/drawableHeight"
            android:layout_margin="8dp"
            android:clickable="true"
            android:focusable="true"
            android:background="@{viewModel.gradientDrawable}"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:padding="8dp"
            app:layout_constraintTop_toBottomOf="@+id/textView"
            app:layout_constraintBottom_toBottomOf="parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">
                <com.google.android.flexbox.FlexboxLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:flexWrap="wrap"
                    app:alignItems="flex_start" >

                    <top.defaults.gradientdrawabletuner.ColorIndicator
                        android:id="@+id/strokeColor"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="stroke color"
                        app:onColorChange="@{(v, color) -> viewModel.strokeColorChanged(color)}"
                        app:color="@{viewModel.drawableProperties.strokeColor}"/>

                </com.google.android.flexbox.FlexboxLayout>

                <android.support.constraint.ConstraintLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content">

                    <TextView
                        android:id="@+id/radiusTitle"
                        android:layout_width="80sp"
                        android:layout_height="wrap_content"
                        android:text='@{"Radius: " + (int)viewModel.drawableProperties.radius}'
                        app:layout_constraintStart_toStartOf="parent"/>

                    <SeekBar
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:max="@{safeUnbox(maxRadiusDp)}"
                        android:onProgressChanged="@{(seekBar, i, b) -> viewModel.radiusChanged(i)}"
                        app:layout_constraintStart_toEndOf="@id/radiusTitle"
                        app:layout_constraintEnd_toEndOf="parent"/>

                </android.support.constraint.ConstraintLayout>
            </LinearLayout>
        </ScrollView>

    </android.support.constraint.ConstraintLayout>
</layout>
